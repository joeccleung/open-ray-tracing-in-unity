#ifndef RED_COMPUTE
#define RED_COMPUTE

// [shader(Red)]

void Red_SecRays(
    inout Ray ray,
    RayHit hit,
    out Ray rays[4]
) 
{
}

float3 Red(
   inout Ray ray,
   RayHit hit,
   float3 ambientLightUpper
)
{
   return float3(1, 0, 0);
}

#endif // Red_COMPUTE 