#ifndef GREEN_COMPUTE
#define GREEN_COMPUTE

float4 DemoMat_color;

// [shader(Green)]

void Green_SecRays(
    inout Ray ray,
    RayHit hit,
    inout SecRaysAtHit secRaysAtHit
) 
{
}

float3 Green(
   inout Ray ray,
   RayHit hit,
   float3 ambientLightUpper,
   float3 secondaryRayColor
)
{
   return DemoMat_color.rgb;
}

#endif // GREEN_COMPUTE 