#ifndef RT_BoundingBox_COMPUTE
#define RT_BoundingBox_COMPUTE

// This struct should match RTBoundingBoxToGPU in RTBoundingBox.cs
struct RTBoundingBox
{
    int leftIdx;
    int rightIdx;
    float3 max;
    float3 min;
};

#endif